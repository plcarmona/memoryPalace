export interface WaitOptions {
    interval?: number;
    timeout?: number;
}
export declare function waitFor(callback: () => boolean | void, { interval, timeout }?: WaitOptions): Promise<void>;
